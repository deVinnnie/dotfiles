org.gradle.daemon=true

# Default value: -Xmx10248m -XX:MaxPermSize=256m
# Prevent stupid gradle from filling up the entire memory.
org.gradle.jvmargs=-Xmx256m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
